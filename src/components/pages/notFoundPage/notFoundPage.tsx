import { MainTemplate } from "../../templates/mainTemplate/mainTemplate";

export const NotFoundPage = () => {
    return (
        <MainTemplate>
            <h1>Not Found</h1>
        </MainTemplate>
    );
};
